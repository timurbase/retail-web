# RetailFlow Web — environment variables
#
# Local dev: copy to .env.local and fill in.
# Railway:   set these in the service Variables tab.

# Backend API base — no trailing slash
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Node env (auto on Railway)
NODE_ENV=development
