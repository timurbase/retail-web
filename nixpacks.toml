# Nixpacks build plan — Bun-based Next.js 16 app.

[phases.setup]
nixPkgs = ["nodejs_20", "bun"]

[phases.install]
cmds = ["bun install --frozen-lockfile"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "bun start -p $PORT"

[variables]
NODE_ENV = "production"
